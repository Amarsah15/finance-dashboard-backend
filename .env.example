# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Replace this with your actual MongoDB connection string in the real .env file
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/finance_dashboard

# Security Configuration
# Replace this with a strong secret key in the real .env file
JWT_SECRET=your_super_secret_jwt_key_here